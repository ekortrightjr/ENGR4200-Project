In agpv_ws workspace:
robot_navigation package (/agpv_ws/src/robot_navigation)
- node: nav2_node
  This node will provide the core navigation functionalities using the nav2 stack and simple commander, to move the robot through the
  stations it patrols, providing higher-level functitons such as: goToStation(), returnToBase(), kill(), etc.

- simple-commander API functions used: BasicNavigator(), setInitialPose(), waitUntilNav2Active(), goToPose(), isTaskComplete(), getFeedback(), followWaypoints(), getResult()

- built and tested simple prototype using turtlebot3

- see task.log for testing so far on physical robot
